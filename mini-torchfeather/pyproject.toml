[project]
name = "mini-torchfeather"
version = "0.1.0"
description = "A minimal torchfeather implementation built from scratch."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false
