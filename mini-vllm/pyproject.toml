[project]
name = "mini-vllm"
version = "0.1.0"
description = "A minimal vLLM implementation built from scratch."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "tqdm>=4.70.0",
    "transformers>=5.16.1",
    "xxhash",
]
